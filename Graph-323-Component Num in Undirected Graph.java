class Solution {
  public int getComNumFromGraph(int n, int[][] edges) {
  	boolean[] visited = new boolean[n]();
  	Arrays.fill(visited, false);

  	for (int i = 0; i < edges.length; i++) {
  		int[] edge = edges[i];
  		int from = edge[0];
  		int to = edge[1];
  		
  	}
  }
}